# 404

You just hit a route that does not exist... the sadness.

<Link to="/">Back</Link>

export const meta = {
  title: '404',
};

import Layout from '../components/Layout.tsx';
export default (props) => <Layout title={meta.title} {...props} />;
