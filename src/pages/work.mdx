---
title: work
---

import { graphql } from 'gatsby';
import WorkList from '../components/WorkList.tsx';

export const pageQuery = graphql`
  query WorkQuery {
    allFile(filter: { sourceInstanceName: { eq: "work" } }) {
      edges {
        node {
          childMdx {
            frontmatter {
              title
              slug
              image {
                childImageSharp {
                  fluid {
                    srcSet
                  }
                }
              }
              tags
            }
          }
        }
      }
    }
  }
`;

# work 🔨

This is a selection of projects I have been a part of.

<WorkList
  data={props.data.allFile.edges.map(({ node }) => node.childMdx)}
></WorkList>
