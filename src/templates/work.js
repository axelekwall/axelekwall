import React from 'react';
import Helmet from 'react-helmet';

import { Page } from '../components/elements';

const Work = () => (
  <Page>
    <Helmet title={'Work'} />
  </Page>
);

export default Work;
